{
    "includes" : ["_aws"],
    "services" : {
        "default_settings" : {
            "params" : {
                "key"    : "key_id",
                "secret" : "key_secret",
                "region" : "us-east-1",
                "base_url" : "http://s3.amazonaws.com",
                "curl.options": {"CURLOPT_PROXY" : "localhost:8080"},
                "scheme" : "http"
            }
        }
    }
}
